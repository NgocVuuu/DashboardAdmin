import React from 'react';

const MapView = () => (
  <div className="map-view" style={{ height: 300, background: '#e0e0e0', borderRadius: 8, textAlign: 'center', lineHeight: '300px' }}>
    Bản đồ bãi đỗ (MapView Placeholder)
  </div>
);

export default MapView;
